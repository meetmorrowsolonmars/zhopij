# zhopij

```shell
cp ./build/.env.example ./build/.env
docker-compose build
docker-compose up -d
make migrate-up
```

```shell
# создали новую ветку
git checkout -b dev
# проверили что поменяли
git status
# добавили изменения
git add .
git commit -m "text"
git push
```
